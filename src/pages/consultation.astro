---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageBreadcrumb from '../components/PageBreadcrumb.astro';
import PageHero from '../components/PageHero.astro';
import { Calendar, CircleCheckBig, Users, Zap } from '@lucide/astro';

// Discussion topics
const discussionTopics = [
  "Your school's current technology infrastructure and digital maturity",
  "Specific challenges around connectivity, budget, and device access",
  "Framework customization for your community and context",
  "Implementation roadmap and phased rollout strategy",
  "Budget planning and cost optimization opportunities",
  "Staff training and digital literacy program design"
];

// Attendee types
const attendees = [
  { icon: Users, title: "School Leadership", description: "Principals, administrators, board members" },
  { icon: Zap, title: "IT & Technology", description: "IT directors, technology coordinators" },
  { icon: Calendar, title: "Academic Leaders", description: "Curriculum directors, instructional coordinators" }
];
---

<BaseLayout
  title="Book a Consultation | nyuchi learning"
  description="Schedule a consultation with nyuchi learning to discuss digital transformation for your school. Get expert guidance on implementing the K-12 Digital Campus Framework."
  keywords="education consultation, school digital transformation consultation, K-12 technology planning, education technology advisory, nyuchi learning consultation, school technology implementation"
>
  <!-- Breadcrumb -->
  <div class="max-w-[1200px] mx-auto px-6 sm:px-8 pt-6">
    <PageBreadcrumb items={[
      { label: 'Book a Consultation' }
    ]} />
  </div>

  <!-- Hero Section -->
  <PageHero
    tagline="Ubuntu: I am because we are"
    title="Book a Consultation"
    subtitle="Let's discuss how the K-12 Digital Campus Framework can transform your school's digital experience"
    share
  />

  <div class="max-w-[1400px] mx-auto px-4 sm:px-6 py-6 sm:py-8">

    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 sm:gap-8 md:gap-12 mb-8 sm:mb-12 md:mb-16">
      <!-- Info Section -->
      <div class="flex flex-col gap-4 sm:gap-6 md:gap-8">
        <!-- What to Expect -->
        <section class="bg-[var(--bg-surface)] p-5 sm:p-6 md:p-8 rounded-xl shadow-sm border border-[var(--border)]">
          <h2 class="text-[var(--primary)] text-2xl font-bold mb-4">What to Expect</h2>
          <p class="text-body text-[var(--text)] mb-4">
            During your consultation, we'll explore how the K-12 Digital Campus Framework can be adapted to your school's unique needs, challenges, and goals. This is a collaborative conversation focused on practical solutions.
          </p>
        </section>

        <!-- We'll Discuss -->
        <section class="bg-[var(--bg-surface)] p-8 rounded-xl shadow-sm border border-[var(--border)]">
          <h3 class="text-[var(--primary)] text-xl font-bold mb-4">We'll Discuss:</h3>
          <ul class="list-none p-0 m-0 space-y-4">
            {discussionTopics.map((topic) => (
              <li class="flex items-start gap-3 text-body">
                <CircleCheckBig class="text-[var(--primary)] flex-shrink-0 mt-0.5" size={20} stroke-width={2} />
                <span class="text-[var(--text)]">{topic}</span>
              </li>
            ))}
          </ul>
        </section>

        <!-- Who Should Attend -->
        <section class="bg-[var(--bg-surface)] p-8 rounded-xl shadow-sm border border-[var(--border)]">
          <h3 class="text-[var(--primary)] text-xl font-bold mb-6">Who Should Attend</h3>
          <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
            {attendees.map((attendee) => (
              <div class="p-6 bg-[var(--bg)] rounded-lg transition-all duration-200 hover:-translate-y-1">
                <attendee.icon class="text-[var(--primary)] mb-4" size={32} stroke-width={1.5} />
                <h4 class="text-lg font-semibold mb-2 text-[var(--text)]">{attendee.title}</h4>
                <p class="text-body text-[var(--text-secondary)] m-0">{attendee.description}</p>
              </div>
            ))}
          </div>
        </section>

        <!-- Commitment -->
        <section class="bg-[var(--bg)] p-8 rounded-xl border-2 border-[var(--primary)]">
          <h3 class="text-[var(--primary)] text-xl font-bold mb-4">Our Commitment</h3>
          <p class="text-body text-[var(--text)] mb-4">
            nyuchi learning is committed to Zimbabwe and Africa's educational future. Built by educators for educators,
            our frameworks are <strong>open, shareable, and adaptable</strong>. We believe in community success—when
            one school thrives, we all benefit.
          </p>
          <p class="font-medium text-[var(--primary)]">
            Money earned internationally is reinvested into Zimbabwe to build, grow, and support education across the continent.
          </p>
        </section>
      </div>

      <!-- Booking Section -->
      <div class="lg:sticky lg:top-[100px] h-fit">
        <div class="bg-[var(--bg-surface)] p-8 rounded-xl border border-[var(--border)]">
          <h2 class="text-[var(--primary)] text-2xl font-bold mb-4">Schedule Your Consultation</h2>
          <p class="text-[var(--text-secondary)] mb-6 text-body">
            Choose a time that works for you. Consultations are typically 45-60 minutes.
          </p>

          <!-- Cal.com Button Link -->
          <div class="mt-8 py-12 px-8 bg-[var(--bg)] rounded-xl">
            <a
              href="https://cal.com/bryanfawcett"
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center justify-center gap-3 text-xl py-5 px-12 min-h-[56px] rounded-full font-semibold bg-[var(--primary)] text-white transition-all duration-300 hover:-translate-y-0.5 hover:shadow-lg hover:brightness-110"
            >
              Open Booking Calendar
            </a>
            <p class="mt-6 text-[var(--text-secondary)] text-meta max-w-[500px]">
              You'll be redirected to our secure booking calendar where you can view available times and schedule your consultation.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- CTA Section -->
    <section class="bg-[var(--bg-surface)] text-[var(--text)] py-16 px-8 rounded-2xl border-2 border-[var(--primary)]">
      <h2 class="text-[var(--text)] text-[clamp(1.75rem,5vw,3rem)] font-bold mb-4">Questions Before Booking?</h2>
      <p class="text-[var(--text)] text-body-lg mb-8 max-w-[65ch]">
        Feel free to explore the framework documentation or read our blog for implementation insights.
      </p>
      <div class="flex gap-4 flex-wrap">
        <a href="/framework" class="btn-primary">View Framework</a>
        <a href="/blog" class="btn-secondary">Read Blog</a>
      </div>
    </section>
  </div>
</BaseLayout>
